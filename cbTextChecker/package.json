{
  "name": "figma-text-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin to find and highlight text strings in TEXT nodes",
  "scripts": {
    "build": "rm -rf dist && npm run build:ui && npm run build:code",
    "build:ui": "vite build --config vite.config.ts",
    "build:code": "vite build --config vite.code.config.ts",
    "fetch:ocr-models": "node scripts/fetch-ocr-models.mjs",
    "pack:share": "npm run build && node scripts/pack-share.mjs",
    "dev": "npm run build && concurrently \"vite build --config vite.config.ts --watch\" \"vite build --config vite.code.config.ts --watch\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.ui.json"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2",
    "vite": "^6.2.2",
    "vite-plugin-singlefile": "^2.1.0",
    "vitest": "^3.0.9"
  },
  "dependencies": {
    "@paddleocr/paddleocr-js": "^0.4.2"
  }
}
