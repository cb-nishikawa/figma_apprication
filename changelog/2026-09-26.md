# 2026-09-26

## cbTextChecker（テキストチェック + OCR 比較）

- **検索・比較画像のフレーム選択と画像選択エリアに「最近選択したフレーム」履歴（最大 20 件）を追加**（cbImageExport と同じ挙動）
  - 検索ターゲット / 比較 A・B / OCR 画像 / 画像比較ターゲットはすべて同じ対象（SECTION・FRAME・INSTANCE・GROUP）を選ぶため、**共通 1 本の履歴**として `figma.clientStorage`（`cbTextChecker.recentTargets`）に永続化（プラグインを閉じても保持・直近が先頭・id 重複は移動）
  - 対象未選択時にクリック（= 選択解決できず `SELECTION_EMPTY`）すると、候補一覧の代わりに **`.target-picker-popover` と同形状の履歴ポップオーバー**を表示し、過去に選んだフレームへ 1 クリックで切り替え可能に
  - 履歴の記録は対象確定時（候補一覧からの選択 / 現在の選択から解決 / 画像書き出し）。ポップオーバー下部の「候補一覧から選ぶ…」で従来の検索一覧にもアクセス可能
  - 起動時に削除済み・対象外ノードを履歴から掃除し、履歴ロード完了後は反映済みの状態を UI へ再送
- 前提の typecheck エラーを修正（`OcrHighlightRegion` に必須の `text` が欠けていた `matchToHoverItem` に補完）