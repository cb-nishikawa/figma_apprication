# 2026-09-24

## ドキュメント運用

- ルートに `AGENTS.md` を追加（作業の正）
- 作業ログをルート `changelog/` に置く方針を確定（`docs/ai/changelog/` ではない）
- `docs/ai/shared-rules.md` を Figma 向けに更新
- `docs/specs/` を追加

## cbImageExport（画像一括書き出しくん）

- 新規プラグイン `cbImageExport/` を追加
- 選択フレーム内の可視画像を一覧・名前検索
- マスク／クリップは親範囲を1件として扱う（[ADR-001](../docs/ai/decisions/ADR-001-image-export-target-resolution.md)）
- PNG / JPG / SVG / PDF の個別・一括・チェック書き出し
- 書き出し倍率を数値入力で設定可能にした（2 = 2倍 / 0.5 = 半分 / 未入力 = 等倍。PNG・JPG に適用）
- 一覧を常に画像ノード単位に変更し、フレーム内の複数画像・マスクされた画像・画像を含むフレームを個別行として表示（[ADR-001](../docs/ai/decisions/ADR-001-image-export-target-resolution.md) 改訂）
- UIを刷新: 検索・一括形式・グローバル倍率を廃止し、画像ごとに倍率と形式を設定（「+」で複数設定可能）。書き出しボタンをヘッダーへ移動し、チェック済み画像のみ書き出し。一覧はサムネイル・対象名・親名・「すべて」チェックを表示
